import { useEffect, useState } from 'react';
import { useParams } from 'react-router-dom';
import axios from 'axios';

function Dashboard () {
    /*
    Features:
    Widget Events for you, based on friends events, interests & promoted events
            links to events and + button to add to going
    Widget Friends' activities to see what events they are going to/they created
            with link to event and + to add to going list
    top: navigation bar, search field for people and events, icon of profile img and name, link to view and edit profile
            seach field with usable parameters
    top: big button 'create event'
    calendar widget to see when events are
            add warning when adding event that collides with already added event:
                replace with last one, add anyway, cancel
    
    */


}

export default Dashboard;